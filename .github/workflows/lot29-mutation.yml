name: Lot 29 critical mutation assurance

on:
  pull_request:
    branches: [main]
    paths:
      - 'config/replay/v2_deterministic_replay_audit_v1.json'
      - 'src/crypto_quant_bot/market_analysis/v2_*replay*audit*.py'
      - 'scripts/run_lot29_v2_deterministic_replay_and_audit.py'
      - 'tests/test_lot29_*.py'
      - '.github/workflows/lot29-mutation.yml'
      - 'requirements-dev.lock'
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  mutation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11.9'
          cache: pip
      - name: Install locked toolchain
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.lock
      - name: Build isolated Lot 29 mutation workspace
        run: |
          rm -rf .tmp-lot29-mutants reports/lot29/mutation
          mkdir -p \
            .tmp-lot29-mutants/src/crypto_quant_bot/market_analysis \
            .tmp-lot29-mutants/scripts \
            .tmp-lot29-mutants/tests \
            reports/lot29/mutation
          cp src/crypto_quant_bot/__init__.py .tmp-lot29-mutants/src/crypto_quant_bot/
          : > .tmp-lot29-mutants/src/crypto_quant_bot/market_analysis/__init__.py
          cp src/crypto_quant_bot/market_analysis/alignment_common.py .tmp-lot29-mutants/src/crypto_quant_bot/market_analysis/
          cp src/crypto_quant_bot/market_analysis/alignment_io.py .tmp-lot29-mutants/src/crypto_quant_bot/market_analysis/
          cp src/crypto_quant_bot/market_analysis/v2_deterministic_replay_and_audit.py .tmp-lot29-mutants/src/crypto_quant_bot/market_analysis/
          cp src/crypto_quant_bot/market_analysis/v2_replay_audit_models.py .tmp-lot29-mutants/src/crypto_quant_bot/market_analysis/
          cp scripts/run_lot29_v2_deterministic_replay_and_audit.py .tmp-lot29-mutants/scripts/
          cp tests/test_lot29_v2_deterministic_replay_and_audit.py .tmp-lot29-mutants/tests/
          cat > .tmp-lot29-mutants/pyproject.toml <<'TOML'
          [project]
          name = "crypto-quant-bot"
          version = "0.29.0.dev0"
          requires-python = "==3.11.*"
          dependencies = []

          [tool.pytest.ini_options]
          pythonpath = ["src", "."]
          addopts = ["--tb=short", "-p", "no:cacheprovider"]

          [tool.mutmut]
          paths_to_mutate = [
            "src/crypto_quant_bot/market_analysis/v2_deterministic_replay_and_audit.py",
            "src/crypto_quant_bot/market_analysis/v2_replay_audit_models.py",
          ]
          tests_dir = ["tests/"]
          runner = "python -m pytest -q tests/test_lot29_v2_deterministic_replay_and_audit.py"
          also_copy = [
            "src/crypto_quant_bot/__init__.py",
            "src/crypto_quant_bot/market_analysis/__init__.py",
            "src/crypto_quant_bot/market_analysis/alignment_common.py",
            "src/crypto_quant_bot/market_analysis/alignment_io.py",
            "scripts/",
          ]
          mutate_only_covered_lines = true
          timeout_factor = 8.0
          TOML
      - name: Run mutation suite
        shell: bash
        run: |
          set -o pipefail
          cd .tmp-lot29-mutants
          mutmut run 2>&1 | tee ../reports/lot29/mutation/run.txt
          mutmut results 2>&1 | tee ../reports/lot29/mutation/results.txt || true
      - name: Enforce mutation score
        run: |
          python - <<'PY'
          import json
          import re
          from pathlib import Path

          path = Path('reports/lot29/mutation/run.txt')
          if not path.is_file():
              raise SystemExit('LOT29_MUTATION_RUN_LOG_MISSING')
          text = path.read_text(encoding='utf-8', errors='replace')
          text = re.sub(r'\x1b\[[0-?]*[ -/]*[@-~]', '', text)
          text = text.replace('\r', '\n').replace('\ufe0f', '')

          counter_pattern = re.compile(r'(\d+)\s*/\s*(\d+)')
          summary_pattern = re.compile(
              r'🎉\s*(\d+)[\s\S]*?⏰\s*(\d+)[\s\S]*?🤔\s*(\d+)[\s\S]*?🙁\s*(\d+)'
          )
          parsed = None
          counters = list(counter_pattern.finditer(text))
          for counter in reversed(counters):
              completed, total = map(int, counter.groups())
              if completed != total or total <= 0:
                  continue
              summary = summary_pattern.search(text, counter.end())
              if summary is None:
                  continue
              killed, timeout, suspicious, survived = map(int, summary.groups())
              evaluated = killed + timeout + suspicious + survived
              if evaluated == total:
                  parsed = (completed, total, killed, timeout, suspicious, survived)
                  break
          if parsed is None:
              raise SystemExit('LOT29_MUTATION_SCORE_PARSE_ERROR')

          completed, total, killed, timeout, suspicious, survived = parsed
          evaluated = killed + timeout + suspicious + survived
          score = 100.0 * (killed + timeout) / evaluated
          payload = {
              'schema_version': 'lot29-critical-mutation-score-v1',
              'scope': 'v2_deterministic_replay_audit',
              'killed': killed,
              'timeout': timeout,
              'survived': survived,
              'suspicious': suspicious,
              'evaluated': evaluated,
              'completed': completed,
              'total': total,
              'score_percent': round(score, 2),
              'minimum_score_percent': 80.0,
              'status': 'PASS' if score >= 80.0 else 'FAIL',
          }
          output = Path('reports/lot29/mutation/score.json')
          output.write_text(json.dumps(payload, indent=2, sort_keys=True) + '\n', encoding='utf-8')
          print(json.dumps(payload, sort_keys=True))
          if score < 80.0:
              raise SystemExit('LOT29_MUTATION_BELOW_80_PERCENT')
          PY
      - name: Verify canonical source immutability
        run: |
          rm -rf .tmp-lot29-mutants
          git diff --exit-code -- src/crypto_quant_bot
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: lot29-critical-mutation-evidence
          path: reports/lot29/mutation/
