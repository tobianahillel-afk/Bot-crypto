{
  "schema_version": 1,
  "policy_kind": "certification_exact_head_binding_v1",
  "semantics": "EXACT_HEAD_TREE_BLOB_AND_CI_EVIDENCE_BINDING_FAIL_CLOSED",
  "lifecycle_policy_source": "config/governance/certification_candidate_lifecycle_v1.json",
  "evidence_policy_source": "engineering/AGENT_CAPABILITIES.json",
  "proof_reuse_policy_source": "config/governance/proof_reuse_policy_v1.json",
  "assurance_policy_source": "config/governance/validation_t3_t4_policy_v1.json",
  "content_hash_algorithm": "sha256",
  "repository_object_hash": "sha1",
  "allowed_git_blob_modes": [
    "100644",
    "100755"
  ],
  "required_ci_evidence_class": "EXACT_GITHUB_ACTIONS_RUN",
  "required_ci_conclusion": "success",
  "max_bound_files": 80,
  "max_ci_runs": 32,
  "candidate_fields": [
    "candidate_id",
    "head_sha",
    "risk_class"
  ],
  "context_fields": [
    "awu_id",
    "scope_base_sha",
    "required_t3",
    "required_t4"
  ],
  "ci_run_fields": [
    "run_id",
    "workflow",
    "head_sha",
    "conclusion"
  ],
  "invariants": [
    "candidate head equals the checked-out Git HEAD when repository input identity is built",
    "input identity includes exact Git tree SHA and sorted tracked blob identities",
    "each bound blob includes Git object SHA SHA-256 digest byte length mode and path",
    "bound working-tree paths must be clean relative to candidate HEAD",
    "path traversal untracked files symlinks non-blob objects and duplicate paths fail closed",
    "CI evidence run ids are unique and every run has success conclusion at the exact candidate head",
    "bundle identity includes candidate input identity and sorted exact CI evidence records",
    "EXACT_HEAD_CERTIFICATION T4 evidence remains forbidden for proof reuse across heads",
    "timestamps prose filenames and mutable external metadata are not certification identity",
    "validation requires only standard library and local Git"
  ]
}
